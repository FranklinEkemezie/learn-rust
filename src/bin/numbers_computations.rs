
// Fill the blanks and fix the errors
fn main() {

    // Integer addition
    assert!(1u32 + 2 == __);

    // Integer subtraction
    assert!(1i32 - 2 == __);
    assert!(1u8 - 2 == -1);

    // Integer multiplication
    assert!(3 * 50 == __);

    // Floating point division
    assert!(9.6 / 3.2 == 3.0);

    // Modulus operation
    assert!(24 % 5 == __);

}